Only in /mnt/data/phase13src/fix2: PHASE14_BATCH2.patch
diff -ru '--exclude=*.md' /mnt/data/phase13base/fix2/packages/api-client/src/index.ts /mnt/data/phase13src/fix2/packages/api-client/src/index.ts
--- /mnt/data/phase13base/fix2/packages/api-client/src/index.ts	2026-09-05 07:48:12.000000000 +0000
+++ /mnt/data/phase13src/fix2/packages/api-client/src/index.ts	2026-09-05 08:03:43.965736056 +0000
@@ -33,11 +33,13 @@
 export interface ApiClientConfig {
   baseUrl?: string;
   headers?: Record<string, string>;
+  timeoutMs?: number;
 }
 
 export class ApiClient {
   private baseUrl: string;
   private defaultHeaders: Record<string, string>;
+  private readonly timeoutMs: number;
 
   constructor(config: ApiClientConfig = {}) {
     this.baseUrl = config.baseUrl || '';
@@ -45,6 +47,9 @@
       'Content-Type': 'application/json',
       ...config.headers,
     };
+    this.timeoutMs = Number.isFinite(config.timeoutMs) && (config.timeoutMs ?? 0) > 0
+      ? Math.floor(config.timeoutMs as number)
+      : 15_000;
   }
 
   async fetch<T>(endpoint: string, options: RequestInit = {}): Promise<T> {
@@ -54,10 +59,20 @@
       ...options.headers,
     };
 
+    const controller = new AbortController();
+    const timeoutId = setTimeout(() => controller.abort(), this.timeoutMs);
+    const callerSignal = options.signal;
+    const onCallerAbort = () => controller.abort(callerSignal?.reason);
+    if (callerSignal) {
+      if (callerSignal.aborted) controller.abort(callerSignal.reason);
+      else callerSignal.addEventListener('abort', onCallerAbort, { once: true });
+    }
+
     try {
       const response = await fetch(url, {
         credentials: options.credentials || 'same-origin',
         ...options,
+        signal: controller.signal,
         headers,
       });
 
@@ -101,8 +116,14 @@
       if (err instanceof ApiClientError) {
         throw err;
       }
+      if (controller.signal.aborted && !callerSignal?.aborted) {
+        throw new ApiClientError(`Request timed out after ${this.timeoutMs} ms`, 'TIMEOUT', 0);
+      }
       const message = err instanceof Error ? err.message : 'Unknown network error';
       throw new ApiClientError(message, 'NETWORK_ERROR', 0);
+    } finally {
+      clearTimeout(timeoutId);
+      if (callerSignal) callerSignal.removeEventListener('abort', onCallerAbort);
     }
   }
 
diff -ru '--exclude=*.md' /mnt/data/phase13base/fix2/server/routes/admin.ts /mnt/data/phase13src/fix2/server/routes/admin.ts
--- /mnt/data/phase13base/fix2/server/routes/admin.ts	2026-09-05 07:50:11.000000000 +0000
+++ /mnt/data/phase13src/fix2/server/routes/admin.ts	2026-09-05 08:03:43.966235923 +0000
@@ -272,7 +272,8 @@
  */
 adminRouter.get('/admin/scans', adminAuthMiddleware, requirePermission('scans:read'), asyncHandler(async (req: Request, res: Response) => {
   const page = parseInt(req.query.page as string, 10) || 1;
-  const limit = parseInt(req.query.limit as string, 10) || 10;
+  const requestedLimit = parseInt(req.query.limit as string, 10);
+  const limit = Number.isFinite(requestedLimit) ? Math.min(100, Math.max(1, requestedLimit)) : 10;
   const country = (req.query.country as string) || undefined;
   const tier = (req.query.tier as string) || undefined;
   const search = (req.query.search as string) || undefined;
@@ -307,7 +308,8 @@
  */
 adminRouter.get('/admin/logs', adminAuthMiddleware, requirePermission('logs:read'), asyncHandler(async (req: Request, res: Response) => {
   const page = parseInt(req.query.page as string, 10) || 1;
-  const limit = parseInt(req.query.limit as string, 10) || 10;
+  const requestedLimit = parseInt(req.query.limit as string, 10);
+  const limit = Number.isFinite(requestedLimit) ? Math.min(100, Math.max(1, requestedLimit)) : 10;
   const eventType = (req.query.eventType as string) || undefined;
   const search = (req.query.search as string) || undefined;
 
@@ -391,7 +393,8 @@
  */
 adminRouter.get('/admin/audit', adminAuthMiddleware, requirePermission('audit:read'), asyncHandler(async (req: Request, res: Response) => {
   const page = parseInt(req.query.page as string, 10) || 1;
-  const limit = parseInt(req.query.limit as string, 10) || 10;
+  const requestedLimit = parseInt(req.query.limit as string, 10);
+  const limit = Number.isFinite(requestedLimit) ? Math.min(100, Math.max(1, requestedLimit)) : 10;
   const search = (req.query.search as string) || undefined;
 
   const result = await dbRepository.getAdminAuditLogsPaginatedAsync({
diff -ru '--exclude=*.md' /mnt/data/phase13base/fix2/server/routes/privacy.ts /mnt/data/phase13src/fix2/server/routes/privacy.ts
--- /mnt/data/phase13base/fix2/server/routes/privacy.ts	2026-09-05 07:48:12.000000000 +0000
+++ /mnt/data/phase13src/fix2/server/routes/privacy.ts	2026-09-05 08:03:43.966145486 +0000
@@ -80,7 +80,9 @@
 privacyRouter.get('/insights/population', async (req, res, next) => {
   try {
     const scoreRaw = Number(req.query.score);
-    if (!Number.isFinite(scoreRaw)) return res.status(400).json({ success: false, error: { code: 'INVALID_SCORE', message: 'A numeric privacy score is required.' } });
+    if (!Number.isFinite(scoreRaw) || scoreRaw < 0 || scoreRaw > 100) {
+      return res.status(400).json({ success: false, error: { code: 'INVALID_SCORE', message: 'Privacy score must be a number between 0 and 100.' } });
+    }
     const result = await dbRepository.getPopulationInsightAsync(scoreRaw, 30);
     const payload: ApiResponse<PopulationInsightResponse> = { success: true, data: result, meta: { timestamp: new Date().toISOString(), requestId: req.requestId || 'req_population' } };
     return res.status(200).json(payload);
diff -ru '--exclude=*.md' /mnt/data/phase13base/fix2/server/services/reverseDns.ts /mnt/data/phase13src/fix2/server/services/reverseDns.ts
--- /mnt/data/phase13base/fix2/server/services/reverseDns.ts	2026-09-05 07:48:12.000000000 +0000
+++ /mnt/data/phase13src/fix2/server/services/reverseDns.ts	2026-09-05 08:03:43.966044771 +0000
@@ -32,14 +32,21 @@
 class ReverseDnsService {
   private cache = new Map<string, { data: ReverseDnsResult; expiresAt: number }>();
   private readonly ttlMs = 60 * 60 * 1000;
+  private readonly maxEntries = 1000;
 
   public async lookup(ip: string): Promise<ReverseDnsResult> {
     const validation = validateIp(ip);
     if (!validation.isValid) throw new Error('Invalid IP address format.');
     if (!validation.isPublic) return { status: 'NOT_MEASURED', names: [], resolver: null, dnssecValidated: null, note: 'Reverse DNS is only queried for publicly routable IP addresses.' };
     const key = validation.normalizedIp;
+    const now = Date.now();
     const cached = this.cache.get(key);
-    if (cached && cached.expiresAt > Date.now()) return cached.data;
+    if (cached && cached.expiresAt > now) {
+      this.cache.delete(key);
+      this.cache.set(key, cached);
+      return cached.data;
+    }
+    if (cached) this.cache.delete(key);
     const controller = new AbortController();
     const timeoutId = setTimeout(() => controller.abort(), 4000);
     try {
@@ -61,7 +68,13 @@
         dnssecValidated: typeof payload.AD === 'boolean' ? payload.AD : null,
         note: names.length ? 'Reverse DNS records resolved through Cloudflare DNS over HTTPS.' : 'No PTR record was returned for this address.',
       };
+      this.cache.delete(key);
       this.cache.set(key, { data: result, expiresAt: Date.now() + this.ttlMs });
+      while (this.cache.size > this.maxEntries) {
+        const oldestKey = this.cache.keys().next().value as string | undefined;
+        if (!oldestKey) break;
+        this.cache.delete(oldestKey);
+      }
       return result;
     } catch (error) {
       const message = error instanceof Error ? error.message : String(error);
diff -ru '--exclude=*.md' /mnt/data/phase13base/fix2/worker/index.ts /mnt/data/phase13src/fix2/worker/index.ts
--- /mnt/data/phase13base/fix2/worker/index.ts	2026-09-05 07:59:01.000000000 +0000
+++ /mnt/data/phase13src/fix2/worker/index.ts	2026-09-05 08:03:43.967874608 +0000
@@ -324,7 +324,11 @@
 function applyPerformanceHeaders(response: Response, startedAt: number, request?: Request): Response {
   const headers = new Headers(response.headers);
   const durationMs = Math.max(0, Number((performance.now() - startedAt).toFixed(1)));
-  headers.set("Server-Timing", `app;dur=${durationMs}`);
+  if (getRequestEnv("NODE_ENV") !== "production") {
+    headers.set("Server-Timing", `app;dur=${durationMs}`);
+  } else {
+    headers.delete("Server-Timing");
+  }
   if (request) headers.set("X-Request-ID", request.headers.get("X-Privasec-Request-ID") || headers.get("X-Request-ID") || "");
   return new Response(response.body, { status: response.status, statusText: response.statusText, headers });
 }
@@ -965,7 +969,6 @@
       pathname === "/api/headers" ||
       pathname === "/api/check/headers" ||
       pathname === "/api/headers/raw" ||
-      pathname === "/api/insights/population" ||
       pathname.startsWith("/api/admin");
     const { client, repo } = needsDatabase
       ? await createRequestDatabase(env)
