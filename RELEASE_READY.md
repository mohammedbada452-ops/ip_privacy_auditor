# Release-ready package

This release uses npm + package-lock.json. `vite` appears once in package.json, and the npm lockfile matches the package manifest. PostgreSQL, Hyperdrive, Express, routes, and auth/admin code are retained.
